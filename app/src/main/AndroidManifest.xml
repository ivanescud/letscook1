<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simplelifestudio.letscook1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.Top10"
            android:label="Top 10"
            android:theme="@style/HomeActivityAppTheme"></activity>
        <activity android:name=".controller.ComentarioActivity" />
        <activity android:name=".controller.SplashRecetaDetail" /> <!-- LandScape repoductor_horizontalActivity -->
        <activity android:name=".controller.reproductor_horizontalActivity" />
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.RegisterActivity" />
        <activity
            android:name=".controller.resultado_listActivity"
            android:label="LET&apos;S COOK"
            android:theme="@style/HomeActivityAppTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".controller.MainActivity" />
        <activity android:name=".controller.Splash" />
        <activity android:name=".controller.receta_detailActivity" />
        <activity
            android:name=".controller.HomeActivity"
            android:label="LET&apos;S COOK"
            android:theme="@style/HomeActivityAppTheme">

        </activity>
    </application>

</manifest>